import 'package:flutter/material.dart';

class FaceDetectSucess extends StatelessWidget {
  const FaceDetectSucess({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Face Detact Sucess"),
      ),
      body: Center(
        child: Text("Face Dectet Sucess Fully"),
      ),
    );
  }
}
